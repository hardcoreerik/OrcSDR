# Downloads and releases

Use GitHub Releases for firmware binaries, flash arguments, checksums, data manifests, release notes, and installation commands. Verify SHA-256 before flashing or installing SD data. Generated MP4 walkthroughs are upload-ready artifacts and are not stored in normal Git history.

The Pages site is deployed only from merged `main`. A pull request build validates the guide but cannot publish it.
